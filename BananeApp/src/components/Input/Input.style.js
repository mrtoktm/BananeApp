import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 10,
        margin: 10,
        backgroundColor: 'darkgray',
        borderRadius: 5,
        flexDirection: 'row',
        borderColor: 'black',
        borderWidth: 1
    },
    input: {
        flex: 1,
    },
});